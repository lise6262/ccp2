{% extends 'base.html.twig' %}


{% block body %}

<main>
<div class="overlay">


        <img src="" class="img-full" alt="">
        <div class="closeMe">X</div>
        </div>
<div class="row overflow-auto" id="corps">
 <div class="row row-cols-1 row-cols-md-3 g-4">
  {% for category in categories %}

 

  <div class="col">
    <div class="card h-100">
      <img src="{{asset('front/MEDIAS/photos/')}}{{ category.imageLogoCategories}}" class="card-img-top img-thumbnail" alt="...">
      <div class="card-body" style="color:black;">
        <h5 class="card-title">{{ category.nomCategories }}</h5>
        <p class="card-text">{{ category.descriptionCategories }}</p>
      </div>
      <div class="d-flex justify-content-between align-items-center">
                            <div class="btn-group">
                                <button type="button" class="btn btn-sm btn-outline-secondary">
                                
                                    <a href="{{ path('categories_details', {'id': category.id}) }}">Voir</a>
                               
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

  {% endfor %}
  
    </div>
  </div>
  </main>
 
{% endblock %}
