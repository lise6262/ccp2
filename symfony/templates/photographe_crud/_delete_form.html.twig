<form method="post" action="{{ path('photographe_crud_delete', {'id': photographe.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photographe.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
