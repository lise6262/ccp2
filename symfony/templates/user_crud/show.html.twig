{% extends 'baseAdmin.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="content-wrapper">
<div class="container-fluid">
    <h1>User</h1>
<div class="table-responsive">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>IsVerified</th>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <a href="{{ path('user_crud_index') }}">Retour à la liste</a>

    <a href="{{ path('user_crud_edit', {'id': user.id}) }}">editer</a>

    {{ include('user_crud/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
