{% extends 'baseAdmin.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
<div class="content-wrapper">
<div class="container-fluid">
    <h1>Contact</h1>
<div class="table-responsive">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact.nom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ contact.mail }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact.message }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <a href="{{ path('contact_crud_index') }}">back to list</a>

    <a href="{{ path('contact_crud_edit', {'id': contact.id}) }}">edit</a>

    {{ include('contact_crud/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
