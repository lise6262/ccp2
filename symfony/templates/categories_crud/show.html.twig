{% extends 'baseAdmin.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
<div class="content-wrapper">
<div class="container-fluid">
    <h1>Categories</h1>
<div class="table-responsive">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom categorie</th>
                <td>{{ category.nomCategories }}</td>
            </tr>
            <tr>
                <th>Description courte categorie</th>
                <td>{{ category.descriptionCategories }}</td>
            </tr>
            <tr>
                <th>Description longue categorie</th>
                <td>{{ category.descriptionLongueCategories }}</td>
            </tr>
            <tr>
                <th>Image logo categorie</th>
                <td>{{ category.imageLogoCategories }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <a href="{{ path('categories_crud_index') }}">Retour à la liste</a>

    <a href="{{ path('categories_crud_edit', {'id': category.id}) }}">editer</a>

    {{ include('categories_crud/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
